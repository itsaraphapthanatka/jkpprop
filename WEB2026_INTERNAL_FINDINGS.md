# บันทึกภายใน · บั๊กที่เจอเองระหว่างเก็บงาน Web 2026

ไม่ได้อยู่ในลิสต์ของลูกค้า และ **ไม่ต้องขึ้นหน้า checklist ที่ลูกค้าอ่าน**
(คุณกิตติพงษ์สั่งไว้ 24 ส.ค. 2569 — เก็บเป็นบันทึกภายในอย่างเดียว)

เขียนไว้เพราะสองในสามเป็นบั๊กที่ลูกค้าจะเจอเองแน่ถ้าไม่แก้ และทั้งสามเผยจุดอ่อน
ของวิธีทดสอบที่ใช้อยู่ — ควรอ่านก่อนแก้อะไรใกล้ ๆ กันรอบหน้า

---

## 1 · แถว "ระบบไฟ" หายจากตารางรายละเอียด

**commit** `075c326`

ตอนทำตาราง 22 แถวตามที่ลูกค้าสั่ง (ข้อ 3) แถวที่ 17 "ระบบไฟ" ถูกชี้ไปที่คีย์
`power_system` ซึ่งคือ *ขนาดหม้อแปลงไฟฟ้า* — คนละช่องกับ `power_phase` ที่เป็น
"ระบบไฟ" จริง JKPBKK1005 กรอก `3 Phase 30/100 amp (Upgradeable)` ไว้ แต่แถวนั้น
ไม่ขึ้นบนเว็บเลย

**สิ่งที่ต้องจำ** — เทสต์ลำดับแถวที่มีอยู่ผ่านทั้งที่แถวหายไปแล้ว เพราะมันกรอง
คีย์ที่ไม่มีค่าทิ้งก่อนเทียบลำดับ (`.filter((k) => at(rows, k) !== -1)`) พอแถว
หาย มันก็ถูกกรองทิ้งไปด้วย → เพิ่มเทสต์ที่ยืนยันตรง ๆ ว่าทรัพย์ที่กรอกระบบไฟไว้
ต้องมีแถวนั้นจริง (`web/tests/unit/propertySpecs.test.ts`)

## 2 · ผลการกด "ยืนยันเกณฑ์" ไม่เคยถูกส่งกลับมา

**commit** `9ede965`

`Visit.gateConfirmed` ถูกเก็บลงฐานข้อมูลตั้งแต่แรก แต่ `GET /api/visits` ไม่เคย
ส่งค่านี้กลับมาใน DTO หน้าจอจึงขึ้นว่า "ยังไม่ยืนยัน" ใหม่ทุกครั้งที่โหลดหน้า
ทีมต้องกดยืนยันซ้ำทุกรอบ

**สิ่งที่ต้องจำ** — เจอเพราะกำลังทำข้อ 21 (ล็อกปุ่มเปิดดีลจนกว่าจะยืนยันเกณฑ์)
ถ้าไม่เจอ ปุ่มที่เพิ่งล็อกจะไม่มีวันปลด = ส่งงานที่ทำให้ระบบใช้ไม่ได้เลย
บทเรียน: ก่อนล็อกอะไรไว้หลังเงื่อนไข ต้องเช็คก่อนว่าเงื่อนไขนั้นอ่านค่าได้จริง

## 3 · ช่องค้นหาในหน้า Shortlist เป็นช่องเปล่า

**commit** `d288a50`

`<input placeholder="ค้นหาทรัพย์เพิ่ม …">` ไม่มี `value` ไม่มี `onChange` —
พิมพ์แล้วไม่เกิดอะไรขึ้นเลย และรายการข้างล่างคือทรัพย์แปดใบแรกของคลัง
(`pool.slice(0, 8)`) ไม่ได้เกี่ยวกับสิ่งที่พิมพ์

**สิ่งที่ต้องจำ** — นี่คือ "ค้นหาใช้ไม่ได้ครับ" ในข้อ 19 ตรงตัว เป็นบั๊กประเภท
"ของตกแต่งที่ดูเหมือนใช้งานได้" ซึ่งโปรเจกต์นี้เจอซ้ำหลายรอบแล้ว (กล่องอัปโหลด
โลโก้ · ปุ่มเปิดดีลที่เป็นลิงก์เปล่า · ตัวเลือกผลการเข้าชม) ตอนไล่หน้าจอรอบหน้า
ควรเช็คทุก input ว่ามี state ผูกอยู่จริงไหม
