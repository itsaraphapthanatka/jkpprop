# TEST_PLAN.md — แผนการทดสอบเว็บ JKP Property

เอกสารนี้เขียนจาก **ข้อบกพร่องที่เจอจริงในโปรเจกต์นี้** ไม่ใช่เช็คลิสต์ทั่วไป
ทุกหัวข้อด้านล่างมีที่มาจากของที่เคยพังจริงอย่างน้อยหนึ่งครั้ง

---

## 1. สถานะปัจจุบัน

| ชุด | จำนวน | รันด้วย |
|---|---|---|
| Unit (18 ไฟล์) | 365 | `cd web && npm test` |
| E2E (5 ไฟล์ · chromium + mobile) | 194 เคส → 261 การรัน | `cd web && npx playwright test` |
| API rules | รวมใน unit | `tests/api/rules.test.mts` |

**ข้อบังคับก่อนรัน e2e:** ต้อง `npm run build` ก่อนเสมอ เพราะ Playwright ใช้ production build
ถ้ามี `npm start` ตัวเก่าค้างอยู่ที่พอร์ต 3000 มันจะเสิร์ฟ build เก่าและทำให้เทสต์แดงแบบไม่มีเหตุผล
(`lsof -ti:3000 | xargs -r kill -9` ก่อนเริ่ม)

ชุดที่ทำหน้าที่เป็น "ยาม" — ห้ามปิด ห้ามใส่ข้อยกเว้นเพื่อให้ผ่าน:

- `tests/unit/publicCopy.test.ts` — แดงถ้ามีข้อความไทยเขียนตรง ๆ ในคอมโพเนนต์สาธารณะ
- `tests/unit/openapi.test.ts` — แดงถ้ามี endpoint ที่ไม่ได้เขียนเอกสารไว้
- `tests/unit/propertySpecs.test.ts` — แดงถ้าพิกัดจริงหลุดออก payload สาธารณะ
- `tests/unit/mapPins.test.ts` — แดงถ้าหมุดบนแผนที่ไม่ได้อยู่ในจังหวัดที่มันอ้าง

---

## 2. ทีม tester (subagent ในโปรเจกต์)

อยู่ที่ `.claude/agents/` เรียกด้วยชื่อได้เลย

| agent | รับผิดชอบ | เรียกเมื่อ |
|---|---|---|
| `tester-lead` | ตัดสินใจว่าจะทดสอบอะไร มอบหมาย รวมผล | ถามลอย ๆ ว่า "ใช้ได้จริงยัง" |
| `tester-flows` | กดทุกปุ่ม ทุกลิงก์ ทุกฟอร์ม | ทำ/แก้ UI เสร็จ |
| `tester-data-truth` | ตัวเลขบนหน้าเว็บมาจากข้อมูลจริงไหม | แตะสถิติ ตัวกรอง การเรียงลำดับ |
| `tester-privacy` | พิกัด ข้อมูลติดต่อ การปิดบัง ความยินยอม | เพิ่มฟิลด์ · แก้ API สาธารณะ · ฝังบริการภายนอก |
| `tester-i18n` | ไทย/อังกฤษ/จีน | เพิ่มข้อความหรือหน้าใหม่ |
| `tester-release` | deploy ขึ้นจริง และเว็บอื่นบนเครื่องไม่พัง | ทุกครั้งหลัง deploy |

---

## 3. รูปแบบข้อบกพร่องที่โปรเจกต์นี้เป็นซ้ำ ๆ

เรียงตามความถี่ที่เจอจริง — ให้ทดสอบสามอันดับแรกก่อนเสมอ

### 3.1 ปุ่มที่ไม่ทำอะไร (พบมากที่สุด)
เว็บนี้พอร์ตจากไฟล์ดีไซน์ ของที่กดได้จำนวนมากจึงมีแต่หน้าตา
ตัวอย่างจริง: ฟอร์มขอข้อมูลที่ `onSubmit` มีแค่ `setSent(true)` · ปุ่มแชท `href="#"` ·
หัวใจที่เก็บสถานะไว้ในหน้า · เมนูแชร์ที่กดแล้วแค่ปิดตัวเอง · checkbox ที่ไม่มี handler

**วิธีจับ:** ตรวจ *ผลลัพธ์* ของการกด (URL เปลี่ยน / มี request / storage เปลี่ยน / DOM เปลี่ยน)
ไม่ใช่ตรวจว่ามี element อยู่

### 3.2 ตัวเลขที่ฝังไว้ในดีไซน์
"2,000+ ทรัพย์" · "640+ รายการต่อทำเล" · "12 ปีในวงการ" · KPI ที่ไม่มีที่มา
**วิธีจับ:** เทียบตัวเลขบนหน้ากับตัวเลขที่นับได้จาก API/ฐานข้อมูล ทุกตัว

### 3.3 ข้อมูลรั่วเพราะคีย์ไม่ตรง
พิกัดของบ้าน/คอนโด/ที่ดิน/โรงงานเคยหลุดออก API สาธารณะ เพราะฟอร์มเก็บลง `location.map`
ขณะที่ตัวกรองความเป็นส่วนตัวรู้จักแค่ `location_map`
**วิธีจับ:** ค้นหา *ค่าจริง* ในผลลัพธ์ (grep เลขพิกัด/เบอร์โทร) ไม่ใช่ดูแค่ชื่อคีย์

### 3.4 ภาษาไทยหลุดไปหน้า /en /zh
ข้อความ UI แก้ด้วย dictionary ได้ แต่ **ค่าที่มาจากฐานข้อมูล** (ประเภททรัพย์ จังหวัด สถานะ)
ต้องผ่าน `enumLabel`/`provinceLabel` เสมอ

### 3.5 เอกสารที่ไม่ตรงกับโค้ด
นโยบายความเป็นส่วนตัวเคยเขียนว่า "ไม่มีบุคคลภายนอกเลย" ขณะที่หน้าติดต่อฝังแผนที่ Google อยู่
ทุกครั้งที่เพิ่มบริการภายนอก ต้องแก้ `prisma/legalPages.ts` ทั้งสามภาษา

---

## 4. บทเรียนเรื่องการเขียนเทสต์ (ห้ามทำซ้ำ)

1. **เทสต์ที่ไม่มีทางแดง ไม่ได้พิสูจน์อะไร**
   เคยมีเทสต์อ่าน `viewBox` จาก `<div>` แล้วเทียบ `null` กับ `null` — ผ่านตลอดไม่ว่าโค้ดจะทำอะไร
   ก่อน commit เทสต์ใหม่ ให้ลองทำให้มันแดงหนึ่งครั้ง
2. **"มองเห็นในทางเทคนิค" ≠ "มองเห็น"**
   ไฮไลต์บนแผนที่เคยผ่านเทสต์ opacity 1 vs 0 แต่จาง จนคนใช้จริงมองไม่เห็น — ใช้ภาพหน้าจอตัดสินเรื่องสายตา
3. **อย่าล็อกค่าที่เป็นการตัดสินใจด้านดีไซน์เป็นตัวเลข**
   เทสต์ที่ล็อก `opacity: 0.35` แดงทุกครั้งที่แต่งหน้าตา — เช็คความสัมพันธ์ ("จางกว่า") แทน
4. **แยก "โค้ดพัง" ออกจาก "ข้อมูลในเครื่องค้าง"**
   เทสต์แดงเพราะทรัพย์ทดสอบ 95 รายการค้างในฐานข้อมูล local, งาน 48 ชิ้นค้าง, Postgres ไม่ได้สตาร์ต
   — เกิดบ่อยมาก ตรวจข้อมูลก่อนแก้โค้ด
5. **เทสต์ที่ทิ้งสถานะไว้ ทำให้ตัวเองแดงในรอบถัดไป**
   เทสต์ที่ปิดสวิตช์แล้วไม่เปิดคืน ผ่านรอบแรก แดงรอบสอง — คืนสถานะเสมอ
6. **ห้ามสร้างข้อมูลจริงบน production**
   ทดสอบฟอร์มบนเว็บจริงให้ดัก `page.route()` แล้ว fulfill เอง เพื่อดู payload โดยไม่เกิด lead ขยะ
7. **ลบไฟล์เทสต์ชั่วคราวทิ้ง**
   เคยมีไฟล์ที่ยิงไปหา production หลุดเข้า repo — ถ้าค้างไว้ CI จะวิ่งไปกวนเว็บจริงทุกครั้ง
   ตั้งชื่อ `zz-*.spec.ts` เสมอ แล้ว `rm` เมื่อเสร็จ

---

## 5. รอบการทดสอบ

### ต่อการแก้ทุกครั้ง (นักพัฒนา)
```bash
cd web
npx tsc --noEmit && npx eslint src        # 0 error เท่านั้น (unused var ทำ build พังบ่อย)
npm test                                   # unit
npm run build && npx playwright test       # e2e (ต้อง build ก่อน)
```

### ก่อน push
- `tester-flows` บนหน้าที่แก้
- ถ้าแตะข้อมูลที่แสดงผล → `tester-data-truth`
- ถ้าแตะฟิลด์/API/บริการภายนอก → `tester-privacy`

### หลัง deploy (บังคับ)
- `tester-release` — image ตรง commit · migration ขึ้น · กวาด 10 vhost · ดิสก์/แรม
- ทดสอบเส้นทางที่เพิ่งแก้บน production จริง โดยไม่สร้างข้อมูล

### รายสัปดาห์
- กวาดทุกหน้าด้วย `tester-flows` (หน้าแรก · รายการ · รายละเอียด · ติดต่อ · FAQ · เกี่ยวกับเรา · เอกสาร · client-shortlist)
- ตรวจข้อมูลตัวอย่างที่ค้างบน production (ตอนนี้เหลือ shortlist 1 · visit 1 · deal 1)
- ตรวจลิงก์โซเชียลใน `/admin/company` ว่ายังเป็นค่าตัวอย่างจากเทมเพลตอยู่ไหม

---

## 6. พื้นที่เสี่ยง เรียงตามผลกระทบต่อธุรกิจ

| ลำดับ | พื้นที่ | ถ้าพังแล้วเกิดอะไร | ครอบคลุมแล้ว |
|---|---|---|---|
| 1 | ฟอร์มรับ lead (หน้าติดต่อ · กล่องในหน้าทรัพย์) | ลูกค้าติดต่อมาแล้วไม่มีใครรู้ | e2e: ส่งจริง · ล้มเหลวต้องบอก |
| 2 | ความเป็นส่วนตัวของข้อมูลทรัพย์/ลูกค้า | ผิด PDPA · เสียความเชื่อถือ | unit + e2e |
| 3 | รายการทรัพย์ · ตัวกรอง · หน้ารายละเอียด | คนหาของไม่เจอ | e2e ครอบคลุมดี |
| 4 | ความยินยอมคุกกี้ + บริการภายนอก | ผิดกฎหมาย · ส่งไอพีผู้ใช้โดยไม่ขอ | e2e 8 เคส |
| 5 | ระบบหลังบ้าน (leads · requirements · deals) | ทีมขายทำงานต่อไม่ได้ | e2e 113 เคส |
| 6 | deploy / เครื่องที่แชร์กับเว็บอื่น 18 ตัว | ทำเว็บคนอื่นล่ม | `tester-release` |

---

## 7. ช่องว่างที่ยังไม่มี (เรียงตามความคุ้ม)

1. **การเข้าถึง (a11y)** — ยังไม่มีการทดสอบอัตโนมัติเลย ควรเริ่มจาก axe-core บน 5 หน้าหลัก:
   คอนทราสต์ · โฟกัสคีย์บอร์ด · ป้ายกำกับของฟอร์ม · ลำดับหัวข้อ
2. **Visual regression** — งานที่ทำบ่อยที่สุดคืองานหน้าตา แต่ไม่มีอะไรจับการเปลี่ยนแปลงที่ไม่ได้ตั้งใจ
   Playwright มี `toHaveScreenshot()` อยู่แล้ว เริ่มจากหน้าแรกกับหน้ารายละเอียด
3. **งบประมาณด้านความเร็ว** — ยังไม่มีเพดานขนาด bundle หรือ LCP · เพิ่งเพิ่ม Leaflet + qrcode เข้าไป
4. **ทดสอบบนอุปกรณ์จริง** — ตอนนี้มีแต่ Pixel 7 จำลอง ยังไม่เคยเปิดบน iPhone จริง
   (Safari ลบ localStorage ทิ้งใน 7 วัน — รายการที่บันทึกไว้และตัวเลือกคุกกี้จะหาย)
5. **ภาษาจีนโดยเจ้าของภาษา** — คำแปลทั้งหมดยังไม่เคยผ่านสายตาคนจีน
6. **ทดสอบโหลด** — เครื่องมี RAM 3.9 GB และ 2 คอร์ ยังไม่รู้ว่ารับได้กี่คนพร้อมกัน

---

## 8. ข้อมูลสำหรับทดสอบ

- **local:** `cd web && npx tsx prisma/seed.ts` (สร้าง org, ผู้ใช้, ทรัพย์ตัวอย่าง)
  เข้าระบบ: `owner@jkp.local` / `jkp12345`
- **production:** ห้ามสร้าง ห้ามแก้ ห้ามลบ ยกเว้นเจ้าของงานสั่งเป็นครั้ง ๆ ไป
  การเข้าหลังบ้าน production ทำโดยสร้างแถว `Session` ชั่วคราวในฐานข้อมูล (เก็บ sha256 ของ token)
  แล้วลบทิ้งทันทีที่เสร็จ
