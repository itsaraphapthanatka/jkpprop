# GENERATED FILE — do not edit by hand.
# Source: web/src/lib/openapi.ts · regenerate with `npm run openapi`
# Coverage is enforced by web/tests/unit/openapi.test.ts
openapi: 3.1.0
info:
  title: JKP Property API
  version: 1.0.0
  description: |-
    API ของแพลตฟอร์มนายหน้าอสังหาริมทรัพย์อุตสาหกรรม JKP Property

    **การยืนยันตัวตน** — session cookie (`jkp_session`, httpOnly) ที่ได้จาก `POST /api/auth/login`
    ทุก endpoint ต้องมี session ยกเว้นที่ระบุว่า *public*

    **รูปแบบข้อผิดพลาด** — เหมือนกันทั้งระบบ: `{ error: { code, message, fields? } }`
    โดย `message` เป็นภาษาไทยพร้อมแสดงให้ผู้ใช้เห็น

    **RBAC** — บังคับที่ชั้น API เสมอ ไม่ใช่แค่ซ่อนปุ่ม สิทธิ์จริง = บทบาท ∧ ขอบเขตข้อมูล ∧ สิทธิ์พิเศษ
    ดูรายละเอียดที่ `FRONTEND_API_SPEC.md` §12 และ `web/BACKEND.md`
servers:
  - url: /
    description: เซิร์ฟเวอร์เดียวกับที่เสิร์ฟหน้าเว็บ
security:
  - cookieAuth: []
tags:
  - name: Auth
    description: เข้าสู่ระบบ · ออกจากระบบ · ข้อมูลผู้ใช้ปัจจุบัน
  - name: Public
    description: เรียกได้โดยไม่ต้องล็อกอิน — มี rate limit และไม่คืนข้อมูลภายใน
  - name: Properties
    description: ทรัพย์และประกาศ
  - name: Leads
    description: ลูกค้าที่สนใจ และงานติดตาม
  - name: Pipeline
    description: Shortlist → Visit → Deal
  - name: Leases
    description: สัญญาเช่าและการแจ้งเตือน
  - name: Media
    description: คลังไฟล์และลายน้ำ
  - name: Content
    description: CMS · sections · SEO · branding
  - name: Config
    description: field schema · ประเภททรัพย์ · พื้นที่ · social
  - name: Admin
    description: ผู้ใช้ · สิทธิ์ · audit log · dashboard
components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: jkp_session
  schemas:
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
              description: รหัสข้อผิดพลาดคงที่ เช่น VALIDATION, FORBIDDEN, AVAILABILITY_REQUIRED
            message:
              type: string
              description: ข้อความภาษาไทย พร้อมแสดงให้ผู้ใช้
            fields:
              type: object
              additionalProperties: &a1
                type: string
              description: ข้อผิดพลาดรายฟิลด์ สำหรับ map เข้า input
          required:
            - code
            - message
      required:
        - error
    Property:
      type: object
      properties:
        id: *a1
        publicCode:
          type: string
          description: รหัสสาธารณะ ออกโดย server จากจังหวัด · แก้ไม่ได้หลังสร้าง
        typeKey: *a1
        typeLabel: *a1
        title: *a1
        status:
          type: string
          enum:
            - draft
            - active
            - hidden
            - archived
        values:
          type: object
          description: ค่าทุกฟิลด์ keyed ด้วย FieldDef.key
        location: *a1
        area:
          type:
            - number
            - "null"
        ownerId: &a3
          type:
            - string
            - "null"
        createdAt: &a2
          type: integer
        updatedAt: *a2
    Lead:
      type: object
      properties:
        id: *a1
        createdAt: *a2
        name: *a1
        phone:
          type: string
          description: ปิดบังเป็นค่าเริ่มต้น เว้นแต่ผู้เรียกมีสิทธิ์ pii
        email: *a1
        piiMasked:
          type: boolean
          description: true = ค่าที่ได้ถูกปิดบังไว้
        company: *a1
        respondentType: *a1
        message: *a1
        typeKey: *a1
        typeLabel: *a1
        dealIntent: *a1
        req:
          type: array
          items:
            type: object
            properties:
              k: *a1
              v: *a1
        source: *a1
        status: *a1
        assigneeId: *a3
        agentName: *a3
    MediaAsset:
      type: object
      properties:
        id: *a1
        name: *a1
        mime: *a1
        size: *a2
        src:
          type: string
          description: URL ของไฟล์ที่ใส่ลายน้ำแล้ว — ไฟล์เดียวที่หน้าเว็บเห็น
        watermarkType:
          type: string
          enum:
            - none
            - corner
            - tiled
        createdAt: *a2
    UserPermissions:
      type: object
      properties:
        id: *a1
        name: *a1
        email: *a1
        role:
          type: string
          enum:
            - owner
            - manager
            - agent
            - co_agent
            - ops
            - marketing
            - translator
        scope:
          type: string
          enum:
            - own
            - all
        privileges:
          type: array
          items:
            type: string
            enum:
              - pii
              - publish
              - price
              - deal_unlock
              - internal_note
              - export
              - audit
        expiresAt: *a3
        mustChangePassword: &a4
          type: boolean
paths:
  /api/auth/login:
    post:
      tags:
        - Auth
      summary: เข้าสู่ระบบ
      security: &a5 []
      description: ตั้ง session cookie · ล็อก 15 นาทีหลังผิด 5 ครั้ง · ข้อความผิดพลาดเหมือนกันทุกกรณีเพื่อไม่ให้เดาได้ว่าอีเมลมีอยู่จริง
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email: *a1
                password: *a1
              required:
                - email
                - password
      responses:
        "200":
          description: เข้าสู่ระบบสำเร็จ
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
                  user:
                    $ref: "#/components/schemas/UserPermissions"
                  mustChangePassword:
                    type: boolean
                    description: true = ใช้รหัสชั่วคราวอยู่ ต้องตั้งรหัสใหม่ก่อนใช้งาน
        "400":
          description: กรอกไม่ครบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: อีเมลหรือรหัสผ่านไม่ถูกต้อง
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: พยายามผิดหลายครั้ง — ถูกล็อกชั่วคราว
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/auth/logout:
    post:
      tags:
        - Auth
      summary: ออกจากระบบ
      responses:
        "200":
          description: ล้าง session แล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
  /api/me:
    get:
      tags:
        - Auth
      summary: โปรไฟล์ของบัญชีที่ล็อกอินอยู่
      responses:
        "200":
          description: ข้อมูลโปรไฟล์
          content:
            application/json:
              schema:
                type: object
        "401":
          description: ยังไม่ได้เข้าสู่ระบบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      tags:
        - Auth
      summary: แก้ข้อมูลติดต่อของตัวเอง
      description: แก้ได้เฉพาะ ชื่อ · เบอร์โทร · LINE · ที่อยู่ — บทบาทและสิทธิ์ไม่อยู่ในนี้ ต่อให้ส่งมาก็ไม่ถูกอ่าน
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400":
          description: ข้อมูลไม่ถูกต้อง
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: ยังไม่ได้เข้าสู่ระบบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/team:
    get:
      tags:
        - Auth
      summary: รายชื่อและข้อมูลติดต่อคนในทีม
      description: เฉพาะบัญชีที่ยังใช้งานอยู่ · ไม่ส่งบทบาทเชิงสิทธิ์ ขอบเขต หรือวันหมดอายุออกไป
      responses:
        "200":
          description: รายชื่อทีม
          content:
            application/json:
              schema:
                type: object
        "401":
          description: ยังไม่ได้เข้าสู่ระบบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/me/permissions:
    get:
      tags:
        - Auth
      summary: ผู้ใช้ปัจจุบัน + สิทธิ์ที่ใช้ได้จริง
      description: สิทธิ์ที่คืนมาผ่านการกรอง FORBIDDEN_PRIVS แล้ว — frontend ใช้ตัดสินว่าจะซ่อนเมนูไหน
      responses:
        "200":
          description: ข้อมูลผู้ใช้
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPermissions"
        "401":
          description: ยังไม่ได้เข้าสู่ระบบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/me/password:
    post:
      tags:
        - Auth
      summary: ตั้งรหัสผ่านของตัวเอง
      description: ล้าง mustChangePassword และเตะ session อื่นทั้งหมด — รหัสชั่วคราวที่เคยส่งต่อจะใช้ไม่ได้ทันที
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                currentPassword: *a1
                newPassword:
                  type: string
                  minLength: 8
                confirmPassword: *a1
              required:
                - currentPassword
                - newPassword
      responses:
        "200":
          description: เปลี่ยนแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400":
          description: ไม่ผ่านการตรวจสอบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: ยังไม่ได้เข้าสู่ระบบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/leads:
    post:
      tags:
        - Public
      summary: ส่งความต้องการจากฟอร์มหน้าเว็บ
      security: *a5
      description: "มี rate limit และ honeypot · คืนแค่ `{ ok: true }` ไม่คืนข้อมูลภายในใด ๆ"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: *a1
                phone: *a1
                email: *a1
                company: *a1
                respondentType: *a1
                message: *a1
                typeKey: *a1
                typeLabel: *a1
                dealIntent: *a1
                req:
                  type: array
                  items:
                    type: object
                    properties:
                      k: *a1
                      v: *a1
                website:
                  type: string
                  description: honeypot — ต้องเว้นว่าง
              required:
                - name
                - phone
                - respondentType
      responses:
        "200":
          description: รับแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400":
          description: ข้อมูลไม่ครบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: ส่งถี่เกินไป
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/listings:
    get:
      tags:
        - Public
      summary: รายการประกาศสำหรับหน้าเว็บ
      security: *a5
      description: เฉพาะทรัพย์ที่เผยแพร่ · ไม่ส่งพิกัด ข้อมูลผู้ให้เช่า หรือฟิลด์ internalOnly
      parameters:
        - name: deal
          in: query
          required: false
          description: rent | sale
          schema:
            type: string
        - name: type
          in: query
          required: false
          description: typeKey เช่น warehouse
          schema:
            type: string
        - name: province
          in: query
          required: false
          description: ชื่อจังหวัด
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: จำนวนสูงสุด (ไม่เกิน 60)
          schema: *a2
      responses:
        "200":
          description: รายการประกาศ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                  total: *a2
  /api/public/properties/{code}:
    get:
      tags:
        - Public
      summary: รายละเอียดทรัพย์ตาม public_code
      security: *a5
      description: ไม่ส่งพิกัดแม่นยำเด็ดขาด (FR-LST-02) และตัดข้อมูลผู้ให้เช่า/โน้ตภายในออก
      parameters:
        - name: code
          in: path
          required: true
          description: public_code เช่น JKP-SPK0042
          schema:
            type: string
      responses:
        "200":
          description: รายละเอียดทรัพย์
          content:
            application/json:
              schema:
                type: object
        "404":
          description: ไม่พบทรัพย์นี้
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/shortlists/{token}:
    get:
      tags:
        - Public
      summary: Shortlist ที่แชร์ให้ลูกค้าดูผ่าน token
      security: *a5
      parameters:
        - name: token
          in: path
          required: true
          description: token ในลิงก์ที่ส่งให้ลูกค้า
          schema:
            type: string
      responses:
        "200":
          description: รายการที่แชร์
          content:
            application/json:
              schema:
                type: object
        "404":
          description: ไม่พบ หรือลิงก์หมดอายุ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: เรียกถี่เกินไป
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags:
        - Public
      summary: ลูกค้าให้ความเห็นต่อทรัพย์หนึ่งรายการ
      security: *a5
      description: หน้า client เคยมีปุ่มให้กดแต่ไม่มีที่ส่ง ทีมจึงต้องโทรถามซ้ำในสิ่งที่ลูกค้าตอบไปแล้ว · เขียนได้เฉพาะ feedback ของรายการที่อยู่ใน shortlist ของ token นั้น
      parameters:
        - name: token
          in: path
          required: true
          description: token ของ shortlist
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                itemId: *a1
                feedback:
                  type: string
                  enum:
                    - interested
                    - maybe
                    - not_interested
                note: *a1
              required:
                - itemId
                - feedback
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400":
          description: ความเห็นไม่ถูกต้อง
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: ไม่พบรายการ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: เรียกถี่เกินไป
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/properties:
    get:
      tags:
        - Properties
      summary: รายการทรัพย์ + ตัวเลขสรุป
      description: ผู้ใช้ที่ scope เป็น own จะเห็นเฉพาะทรัพย์ของตัวเอง (กรองระดับแถว)
      parameters:
        - name: type
          in: query
          required: false
          description: ประเภท
          schema:
            type: string
        - name: province
          in: query
          required: false
          description: จังหวัด
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: สถานะ
          schema:
            type: string
        - name: q
          in: query
          required: false
          description: ค้นหาจากรหัสหรือชื่อ
          schema:
            type: string
      responses:
        "200":
          description: รายการทรัพย์
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Property"
                  summary:
                    type: object
        "400": &a6
          description: ข้อมูลไม่ถูกต้อง
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401": &a7
          description: ยังไม่ได้เข้าสู่ระบบ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403": &a8
          description: ไม่มีสิทธิ์ทำรายการนี้
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags:
        - Properties
      summary: สร้างทรัพย์ใหม่
      description: server เป็นผู้ออก public_code จากจังหวัด · ประเภทที่ถูกปิดรับของใหม่จะถูกปฏิเสธ
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                typeKey: *a1
                title: *a1
                values:
                  type: object
                status: *a1
              required:
                - typeKey
                - title
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Property"
        "400": *a6
        "401": *a7
        "403": *a8
  /api/properties/{id}:
    get:
      tags:
        - Properties
      summary: ทรัพย์รายตัว (รับ id หรือ public_code)
      parameters:
        - name: id
          in: path
          required: true
          description: id หรือ public_code
          schema:
            type: string
      responses:
        "200":
          description: ทรัพย์
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Property"
        "400": *a6
        "401": *a7
        "403": *a8
        "404": &a9
          description: ไม่พบข้อมูล
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      tags:
        - Properties
      summary: แก้ไขทรัพย์
      description: public_code แก้ไม่ได้ · แก้ราคาหลังเผยแพร่ต้องมีสิทธิ์ price · ผู้ที่ไม่มีสิทธิ์ internal_note จะไม่ลบโน้ตภายในโดยไม่ตั้งใจ
      parameters:
        - name: id
          in: path
          required: true
          description: id ของทรัพย์
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: *a1
                status: *a1
                values:
                  type: object
              required: []
      responses:
        "200":
          description: แก้ไขแล้ว
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Property"
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Properties
      summary: ลบทรัพย์ (owner / manager)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของทรัพย์
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/listings:
    get:
      tags:
        - Properties
      summary: ประกาศสำหรับหน้า Listings หลังบ้าน
      responses:
        "200":
          description: รายการประกาศ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/listings/{code}:
    patch:
      tags:
        - Properties
      summary: เผยแพร่ / ซ่อนประกาศ
      description: เผยแพร่ต้องมีสิทธิ์ publish และต้องมีชื่อทรัพย์กับรูปอย่างน้อย 1 รูป (PUBLISH_GATE)
      parameters:
        - name: code
          in: path
          required: true
          description: public_code
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - published
                    - draft
                    - hidden
              required:
                - status
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/leads:
    get:
      tags:
        - Leads
      summary: รายการ lead
      description: scope own = เห็นเฉพาะที่ได้รับมอบหมาย · PII ปิดบังเว้นแต่มีสิทธิ์ pii
      parameters:
        - name: status
          in: query
          required: false
          description: สถานะใน pipeline
          schema:
            type: string
        - name: source
          in: query
          required: false
          description: ที่มา
          schema:
            type: string
      responses:
        "200":
          description: รายการ lead
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Lead"
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Leads
      summary: สร้าง lead จากหลังบ้าน
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: *a1
                phone: *a1
                email: *a1
                company: *a1
                source: *a1
                status: *a1
              required:
                - name
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Lead"
        "400": *a6
        "401": *a7
        "403": *a8
  /api/leads/{id}:
    get:
      tags:
        - Leads
      summary: รายละเอียด lead
      description: lead + โน้ต + งานติดตาม + requirement / shortlist / นัดชม ที่ผูกอยู่ · ก่อนหน้านี้ไม่มี endpoint นี้ หน้าจอจึงเขียน timeline กับงานติดตามไว้ตายตัว
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      responses:
        "200":
          description: รายละเอียด lead
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    patch:
      tags:
        - Leads
      summary: เปลี่ยนสถานะ / มอบหมาย agent
      description: pipeline เดินหน้าอย่างเดียว — ถอยกลับได้เฉพาะ owner/manager
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status: *a1
                assigneeId: *a3
              required: []
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Lead"
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Leads
      summary: ลบ lead (เจ้าของระบบเท่านั้น)
      description: requirement · ผลเช็คว่าง · shortlist · โน้ต · งานติดตาม ของ lead นั้นถูกลบตามไปด้วย · ถ้ามีดีลผูกอยู่จะไม่ยอมลบ (409) ต้องจัดการดีลก่อน
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/leads/{id}/notes:
    get:
      tags:
        - Leads
      summary: บันทึกทั้งหมดของ lead
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      responses:
        "200":
          description: รายการบันทึก
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    post:
      tags:
        - Leads
      summary: เพิ่มบันทึก
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                text: *a1
              required:
                - text
      responses:
        "200":
          description: เพิ่มแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/leads/{id}/tasks:
    get:
      tags:
        - Leads
      summary: งานติดตามของ lead
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      responses:
        "200":
          description: รายการงาน
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    post:
      tags:
        - Leads
      summary: เพิ่มงานติดตาม
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: *a1
                due: *a1
              required:
                - title
      responses:
        "200":
          description: เพิ่มแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    patch:
      tags:
        - Leads
      summary: ติ๊กงานเสร็จ / เปลี่ยนชื่องาน
      description: ก่อนหน้านี้ไม่มี — ช่องติ๊กบนหน้าจอจึงเป็นแค่การตกแต่ง ติ๊กแล้วหายเมื่อ refresh
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                taskId: *a1
                done: *a4
                title: *a1
              required:
                - taskId
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Leads
      summary: ลบงานติดตาม
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
        - name: taskId
          in: query
          required: false
          description: id ของงาน
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/leads/{id}/reveal-contact:
    post:
      tags:
        - Leads
      summary: ขอดูเบอร์/อีเมลเต็ม
      description: ต้องมีสิทธิ์ pii · ทุกครั้งที่เรียกจะถูกบันทึกลง audit log ตาม PDPA ม.37
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ lead
          schema:
            type: string
      responses:
        "200":
          description: ข้อมูลติดต่อเต็ม
          content:
            application/json:
              schema:
                type: object
                properties:
                  phone: *a1
                  email: *a1
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/shortlists:
    get:
      tags:
        - Pipeline
      summary: รายการ shortlist
      responses:
        "200":
          description: รายการ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Pipeline
      summary: สร้าง shortlist + ลิงก์ token ให้ลูกค้า
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: *a1
                leadId: *a1
                codes:
                  type: array
                  items: *a1
              required:
                - codes
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
                  token: *a1
                  url: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/shortlists/{id}:
    get:
      tags:
        - Pipeline
      summary: shortlist รายตัว พร้อมสถานะว่างของแต่ละทรัพย์
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ shortlist
          schema:
            type: string
      responses:
        "200":
          description: รายละเอียด
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    patch:
      tags:
        - Pipeline
      summary: ส่งให้ลูกค้า / จัดลำดับ / แก้โน้ต / เพิ่ม-ลบทรัพย์
      description: ส่ง (status=sent) จะถูกปฏิเสธด้วย AVAILABILITY_REQUIRED ถ้ามีทรัพย์ที่ไม่ว่างแล้ว (FR-AVL-04)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ shortlist
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - open
                    - sent
                    - closed
                order:
                  type: array
                  items: *a1
                notes:
                  type: object
                  additionalProperties: *a1
                addCodes:
                  type: array
                  items: *a1
                removeIds:
                  type: array
                  items: *a1
              required: []
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/visits:
    get:
      tags:
        - Pipeline
      summary: แผนการเข้าชมทั้งหมด
      responses:
        "200":
          description: รายการ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Pipeline
      summary: สร้างแผนการเข้าชม
      description: ส่ง requirementId มาด้วยเพื่อให้แผนนี้ใช้รหัสงานเดียวกับใบงานต้นทาง (REQ-1018) — ถ้าไม่ส่ง ระบบจะเดาให้เฉพาะตอนที่ลูกค้ารายนั้นมีใบงานใบเดียว
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                leadId: *a1
                requirementId: *a1
                date: *a1
                codes:
                  type: array
                  items: *a1
                note: *a1
              required:
                - date
                - codes
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/visits/{id}:
    patch:
      tags:
        - Pipeline
      summary: ยืนยันสถานะว่าง / ปิดแผน / บันทึกผลรายทรัพย์
      description: ปิดแผน (status=done) ต้องยืนยันเกณฑ์กับลูกค้าก่อน (FR-VIS-07) ไม่งั้นได้ GATE_REQUIRED
      parameters:
        - name: id
          in: path
          required: true
          description: id ของแผน
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                gateConfirmed: *a4
                status: *a1
                note: *a1
                outcomes:
                  type: object
                  additionalProperties: *a1
              required: []
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/deals:
    get:
      tags:
        - Pipeline
      summary: รายการดีล
      responses:
        "200":
          description: รายการ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Pipeline
      summary: สร้างดีล
      description: ส่ง visitId มาด้วยเมื่อเปิดจากแผนเข้าชม — ถ้าแผนนั้นยังไม่ได้ยืนยันเกณฑ์จะถูกปฏิเสธ (409) ตามด่านของ Flow D · ดีลจะสืบรหัสงานต่อจากแผนนั้น แล้วแสดงเป็น DEAL-REQ-1018
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: *a1
                leadId: *a1
                requirementId: *a1
                propertyCode: *a1
                amount: &a10
                  type: number
                visitId: *a1
              required:
                - title
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/deals/{id}:
    patch:
      tags:
        - Pipeline
      summary: แก้ไข / ปิดดีล / ปลดล็อก
      description: ปิดดีลจะล็อกฟิลด์การเงิน (FR-DEA-05) · ปลดล็อกต้องมีสิทธิ์ deal_unlock พร้อมเหตุผล และถูกบันทึก audit เสมอ
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - negotiating
                    - won
                    - lost
                amount: *a10
                note: *a1
                unlock: *a4
                reason: *a1
              required: []
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/deals/{id}/offers:
    get:
      tags:
        - Pipeline
      summary: ไทม์ไลน์ข้อเสนอ
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
      responses:
        "200":
          description: รายการข้อเสนอ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    post:
      tags:
        - Pipeline
      summary: บันทึกข้อเสนอใหม่
      description: ถูกปฏิเสธด้วย DEAL_LOCKED ถ้าดีลปิดแล้ว
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                side:
                  type: string
                  enum:
                    - ฝั่งลูกค้า
                    - ฝั่งเจ้าของ
                    - ตกลง
                amount: *a1
                terms: *a1
              required:
                - amount
      responses:
        "201":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/deals/{id}/docs:
    get:
      tags:
        - Pipeline
      summary: เอกสารของดีล
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
      responses:
        "200":
          description: รายการเอกสาร
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    post:
      tags:
        - Pipeline
      summary: แนบเอกสาร (multipart)
      description: PDF หรือรูปภาพ ไม่เกิน 10MB · ดีลที่ปิดแล้วต้องปลดล็อกก่อน · เก็บผ่าน driver เดียวกับคลังสื่อ
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                status: *a1
              required:
                - file
      responses:
        "201":
          description: แนบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/deals/{id}/docs/{docId}:
    get:
      tags:
        - Pipeline
      summary: ดาวน์โหลดเอกสาร
      description: ต้องเข้าสู่ระบบ — เอกสารดีลไม่เปิดสาธารณะ
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
        - name: docId
          in: path
          required: true
          description: id ของเอกสาร
          schema:
            type: string
      responses:
        "200":
          description: ไฟล์
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    patch:
      tags:
        - Pipeline
      summary: เปลี่ยนสถานะเอกสาร (รอเซ็น / ครบ)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
        - name: docId
          in: path
          required: true
          description: id ของเอกสาร
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - รอเซ็น
                    - ครบ
              required:
                - status
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Pipeline
      summary: ลบเอกสาร
      description: owner + manager · ลบทั้งแถวและไฟล์
      parameters:
        - name: id
          in: path
          required: true
          description: id ของดีล
          schema:
            type: string
        - name: docId
          in: path
          required: true
          description: id ของเอกสาร
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/search:
    get:
      tags:
        - Admin
      summary: ค้นหารวม (ทรัพย์ / lead / requirement / deal) — ช่องค้นหาบนแถบบน
      parameters:
        - name: q
          in: query
          required: false
          description: คำค้น อย่างน้อย 2 ตัวอักษร
          schema:
            type: string
      responses:
        "200":
          description: ผลการค้นหา
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/leases:
    get:
      tags:
        - Leases
      summary: สัญญาเช่า
      parameters:
        - name: status
          in: query
          required: false
          description: active (ค่าเริ่มต้น) หรือ all
          schema:
            type: string
      responses:
        "200":
          description: รายการสัญญา
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Leases
      summary: บันทึกสัญญาใหม่ (owner/manager/ops) — รหัสทรัพย์ต้องมีอยู่จริง
      responses:
        "201":
          description: สัญญาที่สร้าง
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/leases/{id}:
    patch:
      tags:
        - Leases
      summary: แก้ไข / ต่อสัญญา / ปิดสัญญา
      parameters:
        - name: id
          in: path
          required: true
          description: id ของสัญญา
          schema:
            type: string
      responses:
        "200":
          description: สัญญาหลังแก้ไข
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    delete:
      tags:
        - Leases
      summary: ลบสัญญา (owner/manager)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของสัญญา
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/notify-config:
    get:
      tags:
        - Leases
      summary: เกณฑ์แจ้งเตือน + รายการที่อ่านแล้ว
      description: เกณฑ์เดือนเป็นค่าระดับองค์กร ส่วน readIds เป็นของผู้ใช้แต่ละคน
      responses:
        "200":
          description: ค่าตั้ง
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    put:
      tags:
        - Leases
      summary: บันทึกเกณฑ์แจ้งเตือน (owner / manager / ops)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: *a4
                months:
                  type: array
                  items: *a2
                includeExpired: *a4
              required:
                - enabled
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/notifications/read:
    post:
      tags:
        - Leases
      summary: ทำเครื่องหมายว่าอ่านแล้ว
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ids:
                  type: array
                  items: *a1
              required:
                - ids
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  readIds:
                    type: array
                    items: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/media:
    get:
      tags:
        - Media
      summary: รายการไฟล์ในคลัง
      responses:
        "200":
          description: รายการไฟล์
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/MediaAsset"
                  totalBytes: *a2
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Media
      summary: อัปโหลดไฟล์ + เลือกลายน้ำ
      description: ต้นฉบับถูกเก็บแยก และ src ที่คืนมาคือไฟล์ที่ใส่ลายน้ำแล้ว (FR-ADM-09) · สูงสุด 10MB
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                watermarkType:
                  type: string
                  enum:
                    - none
                    - corner
                    - tiled
              required:
                - file
      responses:
        "200":
          description: อัปโหลดแล้ว
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaAsset"
        "400": *a6
        "401": *a7
        "403": *a8
  /api/media/{id}:
    delete:
      tags:
        - Media
      summary: ลบไฟล์ (owner / manager)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของไฟล์
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/media/{id}/raw:
    get:
      tags:
        - Media
      summary: ดาวน์โหลดไฟล์
      security: *a5
      description: คืนไฟล์ที่ใส่ลายน้ำแล้วเสมอ · ต้นฉบับต้องใช้ ?original=1 และต้องมี session
      parameters:
        - name: id
          in: path
          required: true
          description: id ของไฟล์
          schema:
            type: string
        - name: original
          in: query
          required: false
          description: ใส่ 1 เพื่อขอไฟล์ต้นฉบับ (ต้องล็อกอิน)
          schema:
            type: string
      responses:
        "200":
          description: ไฟล์
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "401":
          description: ขอต้นฉบับโดยไม่ได้ล็อกอิน
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: ไม่พบไฟล์
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/cms:
    get:
      tags:
        - Content
      summary: เนื้อหา CMS + จำนวนต่อหมวด
      parameters:
        - name: kind
          in: query
          required: false
          description: pages | articles | faq | certs
          schema:
            type: string
      responses:
        "200":
          description: รายการเนื้อหา
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Content
      summary: สร้างเนื้อหาใหม่ (owner / marketing)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                kind: *a1
                title: *a1
                slug: *a1
                category: *a1
              required:
                - title
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
                  slug: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/cms/{id}:
    put:
      tags:
        - Content
      summary: บันทึกร่าง / เผยแพร่เนื้อหา
      description: เผยแพร่ต้องมีสิทธิ์ publish · เนื้อหาเก็บแยกตามภาษา (th / en / zh)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของเนื้อหา
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                lang:
                  type: string
                  enum:
                    - th
                    - en
                    - zh
                title: *a1
                body: *a1
                category: *a1
                slug: *a1
                cover: *a3
                links:
                  type: array
                  items: *a1
                status: *a1
              required: []
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Content
      summary: ลบเนื้อหา
      description: "owner + marketing เท่านั้น · หน้าหลัก (pages: home / about / contact) ลบไม่ได้ เพราะเป็นแถวข้อมูลของหน้าที่มีคอมโพเนนต์ของตัวเอง"
      parameters:
        - name: id
          in: path
          required: true
          description: id ของเนื้อหา
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400":
          description: ลบหน้าหลักของเว็บไม่ได้
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401": *a7
        "403": *a8
        "404": *a9
  /api/requirements:
    get:
      tags:
        - Pipeline
      summary: คิว requirement
      description: กรองตามสถานะ และค้นด้วยรหัส / ชื่อ / บริษัท · counts นับจากตารางจริง
      parameters:
        - name: status
          in: query
          required: false
          description: submitted | confirmed | shortlisted | cancelled
          schema:
            type: string
        - name: q
          in: query
          required: false
          description: รหัส REQ- หรือชื่อลูกค้า/บริษัท
          schema:
            type: string
      responses:
        "200":
          description: รายการ requirement + counts ต่อสถานะ
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Pipeline
      summary: สร้าง requirement ให้ lead
      description: สำหรับความต้องการที่รับมาทางโทรศัพท์ · ฟอร์มหน้าเว็บสร้างให้เองอยู่แล้ว
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                leadId: *a1
                dealIntent: *a1
                typeKey: *a1
                usage: *a1
                areaMin: *a2
                areaMax: *a2
                budgetMin: *a2
                budgetMax: *a2
                moveIn: *a1
                needsRor4: *a4
                nearPort: *a4
                pollution: *a1
                note: *a1
                locations:
                  type: array
                  items: *a1
              required:
                - leadId
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/requirements/{id}:
    get:
      tags:
        - Pipeline
      summary: รายละเอียด requirement
      description: รวมผลเช็คความว่างที่บันทึกไว้ และ shortlist ที่สร้างจาก requirement นี้
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
      responses:
        "200":
          description: รายละเอียด
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    patch:
      tags:
        - Pipeline
      summary: แก้เกณฑ์ / ยืนยัน / ยกเลิก / เปิดใหม่
      description: action=confirm เลื่อน lead เป็น requirements_confirmed อัตโนมัติ · action=cancel ต้องระบุทั้งข้อที่เป็นปัญหาและเหตุผล (FR-CRM-07)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                action:
                  type: string
                  enum:
                    - confirm
                    - cancel
                    - reopen
                cancelField: *a1
                cancelReason: *a1
                dealIntent: *a1
                typeKey: *a1
                usage: *a1
                areaMin: *a2
                areaMax: *a2
                budgetMin: *a2
                budgetMax: *a2
                moveIn: *a1
                needsRor4: *a4
                nearPort: *a4
                pollution: *a1
                note: *a1
                locations:
                  type: array
                  items: *a1
              required: []
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Pipeline
      summary: ลบ requirement
      description: owner + manager · ลบไม่ได้ถ้าสร้าง shortlist จาก requirement นี้ไปแล้ว — ใช้ยกเลิกแทน
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/requirements/{id}/candidates:
    get:
      tags:
        - Pipeline
      summary: ทรัพย์ที่น่าจะตรงกับ requirement นี้
      description: พร้อมรูป ขนาด ราคา สถานะว่าง เบอร์ผู้ติดต่อ และเหตุผลที่ยังไม่ตรงเงื่อนไข — ใช้เลือกทรัพย์ก่อนบันทึกผลเช็ค แทนการพิมพ์รหัสเอง · หน้า requirement ยังใช้เส้นนี้เสนอของใกล้เคียงเมื่อเช็คแล้วไม่ว่างทั้งหมด (เด็ค Web 2026 ข้อ 18) · ต้องล็อกอิน เพราะมีข้อมูลผู้ติดต่อ
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
        - name: q
          in: query
          required: false
          description: "คำค้น: ชื่อ รหัส หรือจังหวัด"
          schema:
            type: string
      responses:
        "200":
          description: รายการทรัพย์
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/requirements/{id}/checks:
    post:
      tags:
        - Pipeline
      summary: บันทึกผลเช็คความว่างกับเจ้าของทรัพย์
      description: หนึ่งแถวต่อทรัพย์ต่อ requirement — เช็คซ้ำทับของเดิม (FR-AVL-04)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                code: *a1
                result:
                  type: string
                  enum:
                    - available
                    - unavailable
                note: *a1
              required:
                - code
                - result
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Pipeline
      summary: เอาทรัพย์ออกจากรายการที่เช็ค
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
        - name: code
          in: query
          required: false
          description: รหัสทรัพย์
          schema:
            type: string
      responses:
        "200":
          description: เอาออกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/requirements/{id}/shortlist:
    post:
      tags:
        - Pipeline
      summary: สร้าง shortlist จากทรัพย์ที่ผ่านเงื่อนไขว่าง
      description: "ประตู FR-AVL-04 บังคับที่ server: เข้าได้เฉพาะทรัพย์ที่ผลเช็คล่าสุด = available และยัง active อยู่ · สำเร็จแล้วเลื่อน lead เป็น shortlisted"
      parameters:
        - name: id
          in: path
          required: true
          description: id ของ requirement
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: *a1
              required: []
      responses:
        "201":
          description: สร้าง shortlist แล้ว
          content:
            application/json:
              schema:
                type: object
        "400":
          description: ยังไม่มีทรัพย์ที่เช็คแล้วว่าว่าง
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401": *a7
        "403": *a8
        "404": *a9
  /api/nav-counts:
    get:
      tags:
        - Admin
      summary: ตัวเลขบนเมนู
      description: lead ที่ยังไม่ได้แตะ และ requirement ที่รอตรวจสอบ — เคยเป็นเลขตายตัว 18 กับ 7
      responses:
        "200":
          description: "{ leads, requirements }"
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/sections:
    get:
      tags:
        - Content
      summary: Section ของหน้าเว็บ
      description: ตารางเดียวกันที่ทั้ง Page Builder และหน้า Sections ใช้
      parameters:
        - name: page
          in: query
          required: false
          description: home | about | contact
          schema:
            type: string
      responses:
        "200":
          description: รายการ section
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
    put:
      tags:
        - Content
      summary: บันทึก section ทั้งหน้า (ลำดับใน array = ลำดับที่แสดง)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                page: *a1
                sections:
                  type: array
                  items:
                    type: object
              required:
                - page
                - sections
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/company:
    get:
      tags:
        - Content
      summary: ข้อมูลติดต่อของบริษัท
      security: *a5
      responses:
        "200":
          description: ที่อยู่ เบอร์ อีเมล เวลาทำการ
          content:
            application/json:
              schema:
                type: object
    put:
      tags:
        - Content
      summary: บันทึกข้อมูลบริษัท (owner / marketing)
      description: ที่อยู่ / ที่อยู่แบบสั้น / วันทำการ เป็นอ็อบเจ็กต์ { th, en, zh } · phones เป็นรายการ { number, label } สูงสุด 6 รายการ
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                legalName: *a1
                address: *a1
                shortLocation: *a1
                phones: *a1
                salesEmail: *a1
                generalEmail: *a1
                hoursDays: *a1
                hoursValue: *a1
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/branding:
    get:
      tags:
        - Content
      summary: ธีมของ tenant
      security: *a5
      responses:
        "200":
          description: ค่าธีม
          content:
            application/json:
              schema:
                type: object
    put:
      tags:
        - Content
      summary: บันทึกธีม (owner / marketing)
      description: "รหัสสีต้องเป็นรูปแบบ #RRGGBB เท่านั้น"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                brandName: *a1
                primary: *a1
                accent: *a1
                neon: *a1
                pine: *a1
                font: *a1
                radius: *a1
                logo: *a3
              required: []
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/seo:
    get:
      tags:
        - Content
      summary: สถานะบริการ SEO + ไฟล์ที่อัปโหลด
      responses:
        "200":
          description: สถานะ
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    put:
      tags:
        - Content
      summary: เปิด / ปิดบริการ
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subscribed: *a4
              required:
                - subscribed
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/seo/files/{key}:
    post:
      tags:
        - Content
      summary: อัปโหลด llms.txt / robots.txt
      description: รับเฉพาะ .txt ขนาดไม่เกิน 1MB · เสิร์ฟที่ /llms.txt และ /robots.txt
      parameters:
        - name: key
          in: path
          required: true
          description: llms | robots
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
              required:
                - file
      responses:
        "200":
          description: อัปโหลดแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    delete:
      tags:
        - Content
      summary: ลบไฟล์
      parameters:
        - name: key
          in: path
          required: true
          description: llms | robots
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400": *a6
        "401": *a7
        "403": *a8
  /api/field-schema:
    get:
      tags:
        - Config
      summary: override ของ field schema ทุกประเภท
      responses:
        "200":
          description: override ต่อ typeKey
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/field-schema/{typeKey}:
    put:
      tags:
        - Config
      summary: บันทึก Field Builder (owner / ops)
      description: ฟิลด์ที่ required ปิดไม่ได้ · key ของฟิลด์ที่เพิ่มเองออกโดย server เพื่อไม่ให้ชนกัน
      parameters:
        - name: typeKey
          in: path
          required: true
          description: ประเภททรัพย์ เช่น warehouse
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                disabled:
                  type: array
                  items: *a1
                order:
                  type: array
                  items: *a1
                extra:
                  type: array
                  items:
                    type: object
              required: []
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/property-types/config:
    get:
      tags:
        - Config
      summary: ประเภททรัพย์ที่ปิดอยู่
      security: *a5
      description: ฟอร์มหน้าเว็บต้องอ่านค่านี้ จึงเปิดให้เรียกได้โดยไม่ล็อกอิน
      responses:
        "200":
          description: รายการที่ปิด
          content:
            application/json:
              schema:
                type: object
    put:
      tags:
        - Config
      summary: เปิด / ปิดประเภททรัพย์ (owner)
      description: ต้องเหลือเปิดอย่างน้อย 1 ประเภท
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                disabled:
                  type: array
                  items: *a1
              required:
                - disabled
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/users/assignable:
    get:
      tags:
        - Config
      summary: ผู้ใช้ที่รับมอบหมาย lead ได้ (ผู้ใช้ที่ล็อกอินแล้วทุกคน)
      description: คืนเฉพาะ id/ชื่อ/บทบาท สำหรับเมนูมอบหมาย — /api/users เต็มรูปแบบยังเป็นของ owner เท่านั้น
      responses:
        "200":
          description: รายชื่อผู้รับผิดชอบ
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/properties/translate:
    post:
      tags:
        - Properties
      summary: เติมหัวเรื่อง EN/中文 ให้ทรัพย์ที่ยังไม่มีคำแปล
      description: ประกอบหัวเรื่องจากฟิลด์ของทรัพย์เอง เขียนเฉพาะภาษาที่ยังว่าง ไม่ทับของที่คนเขียนไว้ · ?dry=1 เพื่อดูผลโดยไม่เขียน
      responses:
        "200":
          description: ผลการเติมคำแปล
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/geography:
    get:
      tags:
        - Config
      summary: จังหวัด / อำเภอ / ตำบล / นิคม
      responses:
        "200":
          description: ผังพื้นที่
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    post:
      tags:
        - Config
      summary: เพิ่มพื้นที่ (owner / ops)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                level:
                  type: string
                  enum:
                    - prov
                    - dist
                    - sub
                    - zone
                th: *a1
                en: *a1
                code: *a1
                parent: *a1
                type: *a1
              required:
                - level
                - th
      responses:
        "200":
          description: เพิ่มแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/geography/{id}:
    patch:
      tags:
        - Config
      summary: แก้ชื่อพื้นที่ / สถานะนิคม (owner / ops)
      parameters:
        - name: id
          in: path
          required: true
          description: id ของพื้นที่
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                th: *a1
                en: *a1
                code: *a1
                type: *a1
                active: *a4
              required: []
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
        "400": *a6
        "401": *a7
        "403": *a8
        "404":
          description: ไม่พบพื้นที่นี้
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      tags:
        - Config
      summary: ลบพื้นที่ (owner / ops)
      description: ปฏิเสธเมื่อยังมีพื้นที่ย่อย หรือมีทรัพย์อยู่ในพื้นที่นั้น — ส่ง ?force=1 เพื่อยืนยันลบทั้งที่มีทรัพย์
      parameters:
        - name: id
          in: path
          required: true
          description: id ของพื้นที่
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
                  used: *a2
        "400": *a6
        "401": *a7
        "403": *a8
        "404":
          description: ไม่พบพื้นที่นี้
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/geography/import:
    post:
      tags:
        - Config
      summary: สร้างผังพื้นที่จากที่อยู่ของทรัพย์ที่มีอยู่ (owner / ops)
      description: เพิ่มเฉพาะพื้นที่ที่ยังไม่มี ไม่แก้ชื่อและไม่ลบของเดิม · ?dry=1 เพื่อดูผลโดยไม่เขียน
      responses:
        "200":
          description: ผลการดึงข้อมูล
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/social:
    get:
      tags:
        - Config
      summary: ช่องทางลงประกาศ + สถานะรายประกาศ
      responses:
        "200":
          description: สถานะ social
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/social/{code}:
    put:
      tags:
        - Config
      summary: บันทึกสถานะ + caption ของประกาศ
      description: ส่ง text เป็น null เพื่อกลับไปใช้ข้อความอัตโนมัติ · ไม่เขียนย้อนกลับไปที่ข้อมูลทรัพย์
      parameters:
        - name: code
          in: path
          required: true
          description: public_code ของประกาศ
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                text: *a3
                channels:
                  type: object
              required: []
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400": *a6
        "401": *a7
        "403": *a8
  /api/social/channels:
    post:
      tags:
        - Config
      summary: เพิ่มช่องทาง
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                key: *a1
                label: *a1
              required:
                - key
                - label
      responses:
        "200":
          description: เพิ่มแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    delete:
      tags:
        - Config
      summary: ลบช่องทาง
      description: ลบสถานะของช่องนั้นออกจากทุกประกาศด้วย ไม่ให้เหลือข้อมูลกำพร้า
      parameters:
        - name: key
          in: query
          required: false
          description: key ของช่องทาง
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: *a4
        "400": *a6
        "401": *a7
        "403": *a8
  /api/users:
    get:
      tags:
        - Admin
      summary: รายชื่อผู้ใช้ (owner เท่านั้น)
      responses:
        "200":
          description: รายชื่อ
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/UserPermissions"
        "400": *a6
        "401": *a7
        "403": *a8
  /api/users/invite:
    post:
      tags:
        - Admin
      summary: เชิญผู้ใช้ใหม่ (owner)
      description: ยังไม่มีระบบส่งอีเมล — คืนรหัสผ่านชั่วคราวมาครั้งเดียว และบัญชีจะใช้งานไม่ได้จนกว่าเจ้าตัวจะตั้งรหัสเอง
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email: *a1
                name: *a1
                role: *a1
                expiresAt: *a1
              required:
                - email
                - role
      responses:
        "201":
          description: สร้างแล้ว
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: *a1
                  email: *a1
                  tempPassword: *a1
        "400": *a6
        "401": *a7
        "403": *a8
  /api/nav-menu:
    get:
      tags:
        - Admin
      summary: ลำดับเมนูประเภททรัพย์บนแถบบนสุด
      responses:
        "200":
          description: ลำดับปัจจุบัน — ว่าง = ใช้ลำดับตั้งต้น
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
    put:
      tags:
        - Admin
      summary: บันทึกลำดับเมนู (owner)
      description: key ที่ไม่รู้จักและที่ซ้ำถูกตัดทิ้ง · ประเภทที่ไม่ได้จัดลำดับต่อท้ายเอง ไม่หายจากเมนู
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                order:
                  type: array
                  items: *a1
              required:
                - order
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/users/{id}:
    patch:
      tags:
        - Admin
      summary: แก้อีเมลของผู้ใช้ (owner)
      description: อีเมลคือชื่อผู้ใช้ตอนเข้าสู่ระบบ · ต้องไม่ซ้ำกับบัญชีอื่นทั้งระบบ · เซสชันที่เปิดค้างไม่ถูกตัด
      parameters:
        - name: id
          in: path
          required: true
          description: id ของผู้ใช้
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email: *a1
              required:
                - email
      responses:
        "200":
          description: แก้อีเมลแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
    delete:
      tags:
        - Admin
      summary: ลบบัญชีผู้ใช้ถาวร (owner)
      description: ลบตัวเอง เจ้าของคนสุดท้าย หรือคนที่ยังมีทรัพย์ / lead ค้างอยู่ ไม่ได้ — ต้องโอนออกก่อน · ประวัติใน Audit log ยังอยู่
      parameters:
        - name: id
          in: path
          required: true
          description: id ของผู้ใช้
          schema:
            type: string
      responses:
        "200":
          description: ลบแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/users/{id}/permissions:
    put:
      tags:
        - Admin
      summary: ตั้งสิทธิ์ผู้ใช้ (owner)
      description: server ตรวจซ้ำทั้ง FORBIDDEN_PRIVS, scope ที่ล็อกไว้ และวันหมดอายุของบทบาทภายนอก
      parameters:
        - name: id
          in: path
          required: true
          description: id ของผู้ใช้
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                role: *a1
                scope: *a1
                privileges:
                  type: array
                  items: *a1
                expiresAt: *a1
              required:
                - role
      responses:
        "200":
          description: บันทึกแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/users/{id}/transfer:
    post:
      tags:
        - Admin
      summary: โอนทรัพย์ทั้งหมดของผู้ใช้คนนี้ไปให้อีกคน (owner)
      description: ใช้ตอนมีคนลาออก · ผู้รับโอนต้องเป็นบัญชีที่ยังใช้งานอยู่
      parameters:
        - name: id
          in: path
          required: true
          description: id ของผู้ใช้ที่จะโอนทรัพย์ออก
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                toUserId: *a1
              required:
                - toUserId
      responses:
        "200":
          description: โอนแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/users/{id}/status:
    patch:
      tags:
        - Admin
      summary: เปิด / ปิดใช้งานผู้ใช้ (owner)
      description: ปิดใช้งานจะเตะ session ของผู้ใช้นั้นทันที · ปิดบัญชีตัวเองไม่ได้
      parameters:
        - name: id
          in: path
          required: true
          description: id ของผู้ใช้
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                active: *a4
              required: []
      responses:
        "200":
          description: อัปเดตแล้ว
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
        "404": *a9
  /api/audit:
    get:
      tags:
        - Admin
      summary: Audit log (ต้องมีสิทธิ์ audit)
      parameters:
        - name: entity
          in: query
          required: false
          description: กรองตาม entity
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: สูงสุด 200
          schema: *a2
      responses:
        "200":
          description: รายการ log
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
        "400": *a6
        "401": *a7
        "403": *a8
  /api/dashboard:
    get:
      tags:
        - Admin
      summary: ตัวเลขสรุปหน้า Dashboard
      description: ทุกตัวเลขเคารพ scope ของผู้เรียก · กิจกรรมล่าสุดคืนเฉพาะผู้ที่มีสิทธิ์ audit
      responses:
        "200":
          description: ข้อมูล dashboard
          content:
            application/json:
              schema:
                type: object
        "400": *a6
        "401": *a7
        "403": *a8
