# แผน: แยก JKP ออกมาเป็นผลิตภัณฑ์ + AI ChatBot + ผู้ช่วย AI ในหลังบ้าน

สถานะ: **แผน ยังไม่เริ่มพัฒนา** · เขียนจากการสำรวจโค้ดจริงเมื่อ 17 ส.ค. 2569

---

## 0. คำตอบสั้น

**ทำได้** และฐานเดิมพร้อมกว่าที่คาด — โค้ดนี้ถูกเขียนแบบ "ตั้งค่าได้จากหลังบ้าน" มาตั้งแต่ต้น
(Field Builder, Branding, CMS, Sections, 3 ภาษา, RBAC, Audit) ซึ่งเป็นคุณสมบัติที่ผลิตภัณฑ์ขายหลายรายต้องมีพอดี

สิ่งที่ต้องระวังที่สุดไม่ใช่เรื่อง AI แต่เป็น **การแยกข้อมูลระหว่างลูกค้า** และ **การกู้คืนข้อมูล**
ซึ่งวันนี้ยังไม่มีการพิสูจน์ว่าทำได้จริง

---

## 1. ของเดิมที่ใช้ต่อได้ทันที

| สิ่งที่มี | สภาพ |
|---|---|
| โครงหลายผู้เช่า (multi-tenant) | **24 จาก 32 ตารางมี `orgId` แล้ว** · อีก 8 ตัวเป็นตารางลูก (LeadNote, LeadTask, ShortlistItem, VisitStop, DealOffer, AvailabilityCheck) ที่สืบทอดผ่านแม่ |
| Field Builder | แต่ละองค์กรกำหนดฟิลด์ของทรัพย์เองได้ ต่อประเภท — ขายเป็นจุดเด่นได้เลย |
| Branding | สี/ฟอนต์/มุมโค้ง แยกต่อองค์กร มีผลกับหน้าเว็บจริง |
| 3 ภาษา | ไทย/อังกฤษ/จีน ครบทั้งระบบ พร้อม SEO (canonical, hreflang, sitemap, llms.txt) |
| สิทธิ์ + audit | owner/manager/agent/marketing · การเปิดดูข้อมูลติดต่อถูกบันทึกทุกครั้ง |
| เส้นทางงานขาย | lead → requirement → shortlist → visit → deal → lease ครบเส้น |
| นำเข้าทรัพย์จาก CSV | `npm run import:properties` ตรวจก่อนเขียนเป็นค่าเริ่มต้น |
| ชุดทดสอบ | unit 369 · e2e 268 · เอกสารแผนทดสอบ 104 เคส |
| สายพาน deploy | GitHub Actions → GHCR → VPS อัตโนมัติ พร้อมย้อนกลับด้วย sha |

### หนี้ที่ต้องปิดก่อนขายให้รายที่สอง

1. **3 จุดที่ยังสมมติว่ามีองค์กรเดียว** — `org.findFirst()` ใน `/api/branding`,
   `/api/property-types/config`, `/api/public/leads` · ถ้ามีสองราย ข้อมูลจะไปผิดบ้าน
2. **ยังไม่มีการผูกโดเมนกับองค์กร** — middleware ยังไม่อ่าน host เลย
3. **ข้อมูล JKP ปนอยู่ใน seed** (ชื่อบริษัท เบอร์ อีเมล ทรัพย์ตัวอย่าง) ต้องแยกเป็น tenant สาธิต
4. **ยังไม่มีการพิสูจน์ว่าสำรอง/กู้คืนฐานข้อมูลได้** — ข้อนี้เป็น P0 ของ TEST_CASES.md ที่ยังว่างอยู่
5. **ยังไม่เคยทดสอบว่าข้อมูลข้ามองค์กรรั่วหรือไม่** — ต้องเป็นเทสต์ถาวร ไม่ใช่ตรวจครั้งเดียว

---

## 2. วิธีแยกโค้ดออกมา — เลือกทางไหน

| ทาง | ข้อดี | ข้อเสีย |
|---|---|---|
| ก. fork แล้วแยกขาด (ลูกค้าละ repo) | เริ่มเร็ว ปรับให้ลูกค้าได้อิสระ | แก้บั๊กต้องทำทุก repo · ขายได้ 3 รายก็เอาไม่อยู่ |
| **ข. repo เดียว หลายองค์กรในระบบเดียว** | แก้ที่เดียวได้ทุกราย · ต่อยอด AI ทีเดียวใช้ได้หมด | ต้องปิดหนี้ข้อ 1 ให้หมดก่อน · ต้องระวังข้อมูลข้ามองค์กร |
| ค. core + ธีมต่อลูกค้า (monorepo) | ยืดหยุ่นสุด | ซับซ้อนเกินความจำเป็นตอนนี้ |

**เสนอทาง ข.** เพราะระบบนี้แยกสิ่งที่ต่างกันต่อองค์กรออกมาเป็น "ข้อมูล" แล้ว
(ฟิลด์ สี เนื้อหา บล็อกหน้าเว็บ) ไม่ใช่โค้ด — ซึ่งคือเงื่อนไขที่ทำให้ทางนี้เป็นไปได้

การ "duplicate JKP" จึงหมายถึง: คัดลอก repo ออกมาเป็นผลิตภัณฑ์ (ชื่อใหม่)
แล้ว **JKP กลายเป็นลูกค้ารายแรกของผลิตภัณฑ์นั้น** ไม่ใช่โค้ดคนละชุดที่ต้องดูแลคู่กัน

---

## 3. AI ChatBot (ฝั่งลูกค้าบนเว็บ)

### กติกาข้อเดียวที่ห้ามละเมิด
**ห้ามตอบสิ่งที่ไม่มีในข้อมูล** — ระบบนี้ทั้งระบบสร้างขึ้นเพื่อกำจัดตัวเลขที่ไม่มีที่มา
บอทที่แต่งราคาหรือแต่งทรัพย์ขึ้นมาจะทำลายสิ่งที่ทำมาทั้งหมดในครั้งเดียว

### สถาปัตยกรรมที่เสนอ
```
ผู้ใช้ถาม
  → ระบบดึงบริบท: ทรัพย์ที่เผยแพร่ของ org นั้น + FAQ + นโยบาย
  → โมเดลเรียกเครื่องมือ (tool) แทนการเดา:
       ค้นทรัพย์(เงื่อนไข)   → ใช้ /api/public/listings ที่มีอยู่แล้ว
       ดูรายละเอียด(รหัส)   → ใช้ /api/public/properties/[code]
       ตอบจาก FAQ(คำถาม)
  → ตอบพร้อม "การ์ดทรัพย์จริง" ที่กดเข้าไปดูได้
  → ถ้าไม่มีข้อมูล: บอกว่าไม่มี แล้วเสนอให้ทีมงานติดต่อกลับ
```

**การค้นหา:** เริ่มด้วย `pg_trgm` (มีในเครื่องแล้ว) + full-text ของ Postgres
พอเกินราว 2,000 ทรัพย์ต่อองค์กรค่อยย้ายไป pgvector
> ตรวจแล้ว: **image `postgres:16` ปัจจุบันไม่มี extension `vector`** ถ้าจะใช้ต้องเปลี่ยน image เป็น `pgvector/pgvector`

### ต่อยอดเป็น lead
บทสนทนาที่ได้ชื่อ+เบอร์+ความต้องการ → สร้าง Lead + Requirement ผ่านเส้นทางเดิม (Flow B)
โดยระบุ `source: 'chatbot'` เพื่อวัดผลได้ว่าบอททำเงินได้จริงไหม

### ข้อกฎหมายและความเป็นส่วนตัว
- บทสนทนา = ข้อมูลส่วนบุคคล → ต้องเพิ่มหมวดความยินยอม และแก้นโยบายทั้ง 3 ภาษา
  (มีเทสต์ยาม `legalAccuracy.test.ts` คอยจับอยู่แล้วว่านโยบายต้องตรงกับสิ่งที่ระบบเรียกจริง)
- ข้อมูลออกไปหาผู้ให้บริการโมเดลต่างประเทศ → ต้องเขียนในนโยบาย และเป็นเรื่องที่ลูกค้าองค์กรจะถาม

### ต้นทุนที่ต้องคุม
จำกัดจำนวนข้อความต่อไอพี · แคชคำตอบ FAQ · ตัดบริบทให้สั้น · นับ token ต่อองค์กรเพื่อคิดเงิน

### ช่องทางเพิ่ม
ตลาดไทยคุยผ่าน **LINE OA** เป็นหลัก — บอทตัวเดียวกันควรต่อ LINE ได้ในเฟสถัดไป

---

## 4. ผู้ช่วย AI ในหลังบ้าน — เรียงตามเวลาที่ประหยัดได้จริง

| # | ผู้ช่วย | ประหยัดอะไร | ความยาก |
|---|---|---|---|
| 1 | **ใส่ทรัพย์จากข้อความ/รูป/PDF** — วางข้อความประกาศแล้วเติมฟอร์มให้ | ตอนนี้กรอก 20–40 ช่องต่อทรัพย์ | กลาง |
| 2 | **แปลชื่อ+คำบรรยายเป็น EN/ZH** | ทำมือทีละรายการอยู่ตอนนี้ · คอลัมน์ `i18n` พร้อมแล้ว | ง่าย |
| 3 | **เขียนคำบรรยาย/โพสต์โซเชียล จากฟิลด์จริง** | ต่อยอด `summaryTemplate.ts` ที่มีอยู่ | ง่าย |
| 4 | **อ่านเอกสาร ร.ง.4 / โฉนด** ดึงเลขที่ วันหมดอายุ เนื้อที่ | งานพิมพ์ซ้ำที่ผิดพลาดบ่อย | ยาก (OCR ไทย) |
| 5 | **จับคู่ requirement ↔ ทรัพย์** พร้อมเหตุผลประกอบ | งานคัดของ shortlist | กลาง |
| 6 | **สรุปบทสนทนา/บันทึกโทรศัพท์ลง LeadNote** | ทีมขายไม่ต้องพิมพ์ | กลาง |
| 7 | **ตรวจคุณภาพประกาศก่อนเผยแพร่** (รูปน้อย ราคาผิดปกติ ฟิลด์ว่าง) | กันประกาศที่ไม่พร้อมขึ้นเว็บ | ง่าย |

**กติกาเดียวกันทุกตัว:** AI ผลิต *ร่าง* เสมอ · ต้องมีคนกดยืนยันก่อนบันทึก ·
บันทึกใน audit ว่าค่านี้มาจาก AI และใครเป็นคนรับ

> ข้อ 7 คุ้มที่สุดต่อแรงที่ลง และเชื่อมกับปัญหาจริงที่เจอวันนี้พอดี:
> ทรัพย์ 2 ใน 3 รายการบน production ยังไม่มีรูปสักใบ

---

## 5. ทำให้ขายได้

### แพ็กเกจ (ตัวอย่างเพื่อเริ่มคุย)
| | Starter | Pro | Agency |
|---|---|---|---|
| ผู้ใช้ | 3 | 10 | ไม่จำกัด |
| ทรัพย์ | 100 | 1,000 | ไม่จำกัด |
| โควตา AI/เดือน | 500 ข้อความ | 5,000 | ตามตกลง |
| โดเมนของตัวเอง | – | ✓ | ✓ |
| ChatBot บนเว็บ | – | ✓ | ✓ |
| LINE OA | – | – | ✓ |

### งานระบบที่ต้องมี
- **แยกข้อมูลตามองค์กร** ทุก query + เทสต์ข้ามองค์กรเป็น P0
- **ผูกโดเมน** host → org (CNAME + certbot) — เครื่องนี้ใช้ nginx ร่วมกับ 18 เว็บอยู่แล้ว ทำได้
- **นับโควตาและออกใบแจ้งหนี้** (Stripe หรือ Omise สำหรับไทย) · โควตา AI คิดเป็นเครดิต
- **หน้า onboarding**: สร้างองค์กร → ตั้งสี/โลโก้ → นำเข้าทรัพย์จาก Excel → เผยแพร่
- **สำรองข้อมูลรายวัน + ซ้อมกู้คืน** — ขายไม่ได้ถ้ายังไม่มี

### จุดขายที่มีของจริงรองรับแล้ว
3 ภาษาพร้อม SEO · PDPA และความยินยอมที่ทำงานจริง · Field Builder ·
ปิดบังข้อมูลติดต่อพร้อม audit · llms.txt ให้ AI ภายนอกดึงไปตอบ

---

## 6. ลำดับงาน

| เฟส | เนื้องาน | ประมาณการ |
|---|---|---|
| **0 · ปิดหนี้ก่อนขาย** | สำรอง/กู้คืนพิสูจน์ได้ · ปิดสมมติฐานองค์กรเดียว 3 จุด · เทสต์ข้ามองค์กร · แยกข้อมูล JKP ออกจาก seed | 1–2 สัปดาห์ |
| **1 · ผูกโดเมน + onboarding** | host → org · หน้าเปิดองค์กรใหม่ · นำเข้า Excel | 2 สัปดาห์ |
| **2 · ChatBot อ่านอย่างเดียว** | ตอบ FAQ + ค้นทรัพย์จริง + การ์ดทรัพย์ · 3 ภาษา · จำกัดโควตา | 2–3 สัปดาห์ |
| **3 · ChatBot เก็บ lead** | ต่อเข้า Flow B · วัดผลว่าปิดงานได้จริงไหม | 1 สัปดาห์ |
| **4 · ผู้ช่วยใส่ทรัพย์ + แปล + ตรวจคุณภาพ** | ข้อ 1, 2, 3, 7 ในตารางข้างบน | 3 สัปดาห์ |
| **5 · เก็บเงิน** | แพ็กเกจ · โควตา · ใบแจ้งหนี้ | 2 สัปดาห์ |
| **6 · LINE OA · OCR เอกสาร · จับคู่ requirement** | ตามความต้องการลูกค้าจริง | 4 สัปดาห์+ |

รวมถึงจุดที่ "ขายได้" ประมาณ **8–11 สัปดาห์** ถ้าทำต่อเนื่อง

---

## 7. ความเสี่ยงที่ต้องตัดสินใจก่อนเริ่ม

1. **เครื่องปัจจุบันไม่พอ** — 2 คอร์ / RAM 3.9 GB และแบกเว็บอื่นอีก 18 ตัว
   หลายองค์กร + AI ต้องแยกเครื่อง (แนะนำ 4 คอร์ / 8–16 GB สำหรับผลิตภัณฑ์นี้โดยเฉพาะ)
2. **ผู้ให้บริการโมเดล** — ข้อมูลลูกค้าจะออกนอกประเทศ ต้องเขียนในสัญญาและนโยบาย
   ลูกค้าองค์กรบางรายจะไม่ยอม → ต้องมีทางเลือกหรือปฏิเสธลูกค้ากลุ่มนั้นแต่แรก
3. **ต้นทุน AI ต่อข้อความ vs ราคาที่ขาย** — ต้องคิดก่อนตั้งราคา ไม่ใช่หลังจากมีคนใช้เยอะ
4. **AI แต่งข้อมูล** — ต้องมีชุดคำถามมาตรฐานไว้วัดทุกครั้งที่เปลี่ยนโมเดล/prompt
5. **เจ้าของข้อมูล** — ลูกค้าเลิกใช้แล้วขอข้อมูลคืน/ให้ลบ ต้องทำได้ (PDPA)

---

## 8. คำถามที่ต้องได้คำตอบก่อนลงมือ

1. ขายให้ใคร — นายหน้ารายเล็ก (1–5 คน) หรือบริษัทตัวแทน (20+ คน) · ปีแรกตั้งเป้ากี่ราย
2. งบต่อเดือนสำหรับค่า AI ที่รับได้
3. ChatBot ต้องอยู่บน LINE ตั้งแต่แรกหรือขึ้นเว็บก่อนพอ
4. ทรัพย์ของลูกค้าแต่ละรายแยกขาดจากกัน หรืออนาคตอยากทำเป็นตลาดกลางที่แชร์ทรัพย์กันได้
   (ข้อนี้เปลี่ยนโครงฐานข้อมูลตั้งแต่ต้น ตัดสินใจทีหลังจะแพง)
5. JKP ยังเป็นเจ้าของโค้ดคนเดียว หรือผลิตภัณฑ์นี้จะมีผู้ถือหุ้นอื่น

---

## 9. สิ่งที่จะไม่ทำ (ตั้งใจ)

- ไม่ทำ ChatBot ที่ตอบจากความรู้ทั่วไปของโมเดล — ตอบเฉพาะจากข้อมูลในระบบ
- ไม่ให้ AI เขียนลงฐานข้อมูลเองโดยไม่มีคนยืนยัน
- ไม่ทำ marketplace รวมทรัพย์ข้ามองค์กร จนกว่าจะมีคำตอบข้อ 8.4
- ไม่ย้ายทุกอย่างไป microservice — ระบบเดียวที่แยกข้อมูลถูกต้องเพียงพอสำหรับ 50 องค์กรแรก
